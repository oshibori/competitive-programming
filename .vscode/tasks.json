{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            
            "command": "g++",
            //"args": ["-g3","-O0","-I debug","${fileDirname}\\${fileBasename}","-o ${fileDirname}\\${fileBasenameNoExtension}.exe"],
            "args": ["-g3","-O0","-I debug","${fileDirname}\\${fileBasename}"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}